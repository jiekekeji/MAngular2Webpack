/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CommPipe } from './comm.pipe';

describe('CommPipe', () => {
  it('create an instance', () => {
    const pipe = new CommPipe();
    expect(pipe).toBeTruthy();
  });
});
